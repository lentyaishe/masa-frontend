export enum Gender {
    Female = "Female",
    Male = "Male"
}

export enum Layout {
    Vertical = "vertical",
    Horizontal = "horizontal"
}

export enum LayoutDirection {
    Rtl = "mf-rtl",
    Ltr = "mf-ltr"
}
